<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.slohith.findurway">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity" />
        <activity android:name=".activity401"
                  android:label="401"/>
        <activity android:name=".activity401k"
                  android:label="401 K"/>
        <activity android:name=".activity273c"
                  android:label="273 C"/>
        <activity android:name=".activity401b"
                  android:label="401 K"/>
        <activity android:name=".activity402b"
                  android:label="402 B"></activity>
    </application>

</manifest>